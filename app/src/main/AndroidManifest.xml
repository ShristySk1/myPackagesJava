<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayata.mypackagesjava">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".navigationdrawer.NavigayionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ui.UiActivity">

        </activity>
        <activity android:name=".toolbar.ToolbarActivity"></activity>
        <activity android:name=".ViewPager.ViewPagerActivity" />
        <activity android:name=".AutoCompleteSearch.SearchActivity" />
        <activity android:name=".AutoCompleteSearch.SearchFragmentContainer">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchables" />
        </activity>
        <activity
            android:name=".MotionActivity"
            android:label="@string/title_activity_motion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DialogActivity.DialogActivity" />
        <activity android:name=".ArrayAdapter.ArrayAdapterActivity" />
        <activity android:name=".Room.NoteActivity" />
        <activity android:name=".retrofit.RetrofitActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>